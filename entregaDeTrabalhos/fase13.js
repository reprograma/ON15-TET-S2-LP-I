// Fase 13
turnRight();
for (var count = 0; count < 6; count++) {
  placeBlock("rail");
  moveForward();
}
placeBlock("rail");
turnRight();
for (var count2 = 0; count2 < 6; count2++) {
  placeBlock("rail");
  moveForward();
}
